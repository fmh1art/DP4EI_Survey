%!TEX root = ../../main.tex
% Backup-section template. Files in secs/bk/ are never imported by main.tex.
\section{Sample Backup Section}
\label{sec:sample-backup}

Keep only a short, clearly labeled alternative passage here when an earlier argument may still be useful.
Move accepted prose into a numbered file under \texttt{secs/} and import it from \texttt{main.tex}.
