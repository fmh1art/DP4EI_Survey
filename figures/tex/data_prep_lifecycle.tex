\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  node distance=7mm and 5mm,
  stage/.style={draw=surveyblue!70, rounded corners=2pt, align=center, minimum height=10mm,
    text width=2.65cm, inner sep=3pt, fill=surveylightblue, font=\footnotesize},
  signal/.style={draw=surveyteal!70, rounded corners=2pt, align=center, minimum height=8mm,
    text width=2.65cm, inner sep=3pt, fill=surveylightteal, font=\footnotesize},
  arr/.style={-{Latex[length=2mm]}, thick, draw=black!60},
  feedback/.style={-{Latex[length=2mm]}, thick, dashed, draw=surveyrose}
]
\node[stage] (raw) {\textbf{Raw records}\\robot, human, simulation, generated};
\node[stage, right=of raw] (ops) {\textbf{Preparation}\\align, segment, label, select, synthesize};
\node[stage, right=of ops] (dist) {\textbf{Training distribution}\\units, roles, weights, provenance};
\node[stage, right=of dist] (learner) {\textbf{Learner}\\pretrain, adapt, post-train};
\node[stage, right=of learner] (deploy) {\textbf{Behavioral evidence}\\utility, recovery, safety, shift};

\draw[arr] (raw) -- (ops);
\draw[arr] (ops) -- (dist);
\draw[arr] (dist) -- (learner);
\draw[arr] (learner) -- (deploy);

\node[signal, below=of ops] (guide) {\textbf{Decision signals}\\records, humans, models, execution};
\node[signal, below=of dist] (verify) {\textbf{Utility evidence}\\diagnostic $\rightarrow$ proxy $\rightarrow$ policy $\rightarrow$ robot};
\draw[arr] (guide) -- (ops);
\draw[arr] (verify) -- (dist);
\draw[feedback] (deploy.south) |- ($(verify.south)+(0,-5mm)$) -| (guide.south);
\draw[feedback] (deploy.north) to[out=115,in=65,looseness=1.35]
  node[above,align=center,font=\small] {failure mining, active acquisition,\\mixture and verifier recalibration} (raw.north);
\end{tikzpicture}
\caption{Data preparation transforms raw records into training units, sampling weights, and stage-specific roles. Solid arrows show the forward data path. Dashed arrows show how evaluation changes later preparation decisions.}
\label{fig:lifecycle}
\end{figure*}
